apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gadb

resources:
  - namespace.yaml
  - secrets.yaml
  - postgres.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: gadb
  app.kubernetes.io/part-of: government-accountability-database
